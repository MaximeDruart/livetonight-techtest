import { css } from "styled-components"

const theme = {}

theme.colors = {
  main: "#3CE295",
  title: "#484848",
}

theme.textStyles = {
  body: {
    small: css`
      font-weight: 200;
      font-size: 12px;
    `,
  },
}

export default theme
